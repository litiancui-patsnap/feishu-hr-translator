# 生产环境配置模板
# 部署前请复制此文件为 ../.env 并修改相应配置

# 应用端口（前端 Nginx 监听端口）
WEB_PORT=80

# 应用基础 URL（改为服务器的实际地址）
APP_BASE_URL=http://your-server-ip

# 后端端口（内部使用，不需要修改）
APP_PORT=8080

# ============================================
# 飞书配置（必填）
# ============================================
FEISHU_APP_ID=your_app_id
FEISHU_APP_SECRET=your_app_secret
FEISHU_BOT_VERIFICATION_TOKEN=your_verification_token
FEISHU_BOT_ENCRYPT_KEY=your_encrypt_key
FEISHU_DEFAULT_CHAT_ID=your_chat_id

# ============================================
# AI 模型配置（必填）
# ============================================
DASHSCOPE_API_KEY=your_dashscope_key
QWEN_MODEL=qwen-max
QWEN_API_MODE=text

# ============================================
# 存储配置
# ============================================
STORAGE_DRIVER=csv
CSV_PATH=./data/reports_slim.csv

# 如果使用 Google Sheet
# STORAGE_DRIVER=sheet
# GOOGLE_SERVICE_ACCOUNT_JSON=./secrets/gs.json
# GOOGLE_SHEET_ID=your_sheet_id

# 如果使用飞书多维表格
# STORAGE_DRIVER=bitable
# FEISHU_TENANT_KEY=your_tenant_key
# BITABLE_BASE_ID=your_base_id
# BITABLE_TABLE_ID=your_table_id

# ============================================
# OKR 配置
# ============================================
OKR_SOURCE=cache
OKR_CACHE_PATH=./data/okr_cache.json

# OKR 同步（选填）
# FEISHU_TENANT_APP_ID=your_tenant_app_id
# FEISHU_TENANT_APP_SECRET=your_tenant_app_secret
# FEISHU_OKR_IDS=okr_id1,okr_id2

# ============================================
# 自动同步配置（选填）
# ============================================
# AUTO_SYNC_ENABLED=true
# AUTO_SYNC_TIME=02:00
# AUTO_SYNC_LOOKBACK_HOURS=24
# AUTO_SYNC_RUN_ON_START=false

# ============================================
# 网络配置
# ============================================
REQUEST_TIMEOUT_SECONDS=30
HTTP_TRUST_ENV=false

# ============================================
# 用户认证（Web UI）
# ============================================
# JWT 密钥（请修改为随机字符串，用于签名 token）
JWT_SECRET_KEY=change-this-to-a-random-secret-key-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# 默认管理员账号（首次启动时创建）
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
